# Copy to .env and adjust for local overrides (optional).
# Docker Compose uses inline environment in docker-compose.yml by default.

DATABASE_URL=postgresql://paas_user:paas_pass@localhost:5432/paas_db
JWT_SECRET_KEY=change-this-to-a-long-random-secret
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=60
